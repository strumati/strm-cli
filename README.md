### strm-cli

This project is a command line interface that will be compatible with many Cloud Services

### Running

you need to have installed `node.js`

right now the isn't a buildin alias so you ether have to configure it you self or

use `node strm.js <command> <subcommand>`
